import { ValidateExpiryDirective } from './validate-expiry.directive';

describe('ValidateExpiryDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateExpiryDirective();
    expect(directive).toBeTruthy();
  });
});
